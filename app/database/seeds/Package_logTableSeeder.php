<?php

class Package_logTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('package_log')->truncate();

		$package_log = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('package_log')->insert($package_log);
	}

}
